/**
 * Application-wide route constants
 */
export const ROUTES = {
  AUTH: 'auth',
  USERS: 'users',
  CREATE: 'create'
};

/**
 * API endpoint path constants
 */
export const API_PATHS = {
  AUTH: {
    LOGIN: 'login'
  },
  USERS: {
    BASE: 'users'
  }
};
